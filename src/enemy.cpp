#include "enemy.h"

Enemy::Enemy(int _max_hp, int _max_energy, int _xp_to_lvlup,int _damage, bool _is_enemy)
            : Robot(_max_hp,_max_energy, _xp_to_lvlup, _damage, _is_enemy)
            {
            }